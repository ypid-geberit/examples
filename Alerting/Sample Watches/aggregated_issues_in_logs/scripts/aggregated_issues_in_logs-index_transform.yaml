---

# yamllint disable rule:line-length

script:
  lang: 'painless'
  source: |
    HashMap test = ['test': 23];
    if (params.getOrDefault('return_boolean', false)) {
      return false;
    } else {
      return test;
    }
